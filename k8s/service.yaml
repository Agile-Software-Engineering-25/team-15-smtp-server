apiVersion: v1
kind: Service
metadata:
  name: postfix
  labels:
    app: postfix
spec:
  selector:
    app: postfix
  ports:
    - protocol: TCP
      port: 25
      targetPort: smtp
  type: ClusterIP # change to LoadBalancer if you need external access
